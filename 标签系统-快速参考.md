# Notella 标签系统 - 快速参考指南

## 文件修改清单

### ✨ 新增文件（2个）

1. **`components/TagInput.js`** - 标签输入组件
   - 支持回车/逗号添加标签
   - 自动去重
   - 可删除标签
   - 适配明暗模式

2. **`components/TagFilter.js`** - 标签筛选组件
   - 自动统计标签
   - 显示笔记数量
   - 点击筛选功能
   - 按数量排序

---

### 🔧 修改文件（5个）

#### 1. `context/NotesContext.js`
**新增内容：**
- 状态：`selectedTag` - 当前选中的标签
- 方法：`handleTagsChange(tags, id)` - 处理标签变更
- 增强：`filterNotesBySearchAndTag()` - 支持标题+标签搜索

**关键代码行：**
- Line 16: 添加 selectedTag 状态
- Line 23-60: 增强的搜索和筛选逻辑
- Line 86: 切换视图时清除标签筛选
- Line 98: 新笔记初始化 tags 字段
- Line 186-198: handleTagsChange 方法
- Line 220-222: 导出新的状态和方法

---

#### 2. `components/NotePreviewer.js`
**修改内容：**
- Line 8: 导入 TagInput 组件
- Line 18: 导入 handleTagsChange 方法
- Line 133-181: 重构 bodyContainer 布局，添加标签输入区域

**布局变化：**
```
旧: bodyContainer (flex, scroll)
新: bodyContainer (flex-col)
    ├─ Content Area (flex-1, scroll)
    └─ Tags Section (fixed bottom)
```

---

#### 3. `components/Recipient.js`
**修改内容：**
- Line 5: 导入 TagFilter 组件
- Line 30: 更新搜索框占位符 "Search by title or tags..."
- Line 39: 插入 TagFilter 组件
- Line 51: 传递 tags 属性到 NoteInRecipient

---

#### 4. `components/NoteInRecipient.js`
**修改内容：**
- Line 5: 添加 tags 参数（默认空数组）
- Line 19: 添加 cursor-pointer 类名
- Line 26-50: 显示标签芯片（最多3个）

**标签显示逻辑：**
- 显示前3个标签
- 超过3个显示 "+N"
- 芯片样式，适配主题

---

#### 5. `utils/initialAccountNote.js`
**修改内容：**
- Line 5 (body): 添加标签使用说明
- Line 6: 添加示例标签 `["guide", "documentation", "welcome"]`

---

## 功能使用指南

### 🏷️ 添加标签

1. 选中笔记
2. 滚动到编辑区底部
3. 在 "Tags" 输入框输入标签名
4. 按回车或逗号完成添加
5. 标签自动保存（1秒防抖）

**快捷操作：**
- 回车键：添加标签
- 逗号：添加标签
- Backspace（空输入框）：删除最后一个标签
- 点击 X：删除指定标签

---

### 🔍 标签筛选

1. 查看笔记列表侧边栏
2. 找到 "Filter by Tags" 区域
3. 点击任意标签按钮
4. 列表自动筛选
5. 再次点击或点 "Clear" 取消筛选

**提示：**
- 选中标签会高亮显示（蓝色）
- 数字徽章显示笔记数量
- 标签按笔记数量降序排序

---

### 🔎 搜索功能

**搜索范围：**
- 笔记标题（部分匹配）
- 笔记标签（部分匹配）

**示例：**
```
搜索 "工作" →
✓ 标题: "工作计划" → 匹配（标题包含）
✓ 标题: "会议记录", 标签: ["工作"] → 匹配（标签包含）
✗ 标题: "学习笔记", 标签: ["技术"] → 不匹配
```

---

## 数据结构

### 笔记对象

```javascript
{
  id: "abc123",              // String (nanoid)
  title: "我的笔记",          // String
  body: "笔记内容...",        // String (支持 Markdown)
  tags: ["工作", "重要"]      // Array<String> (新增)
}
```

### MongoDB 存储

```json
{
  "_id": "ObjectId(...)",
  "email": "user@example.com",
  "notes": [
    {
      "id": "abc123",
      "title": "我的笔记",
      "body": "内容",
      "tags": ["工作", "重要"]
    }
  ],
  "trashedNotes": []
}
```

---

## 技术要点

### 状态管理

**NotesContext 新增：**
```javascript
// 状态
const [selectedTag, setSelectedTag] = useState(null);

// 方法
const handleTagsChange = (tags, id) => { ... };

// 导出
value={{
  ...,
  selectedTag,
  setSelectedTag,
  handleTagsChange
}}
```

---

### 搜索逻辑

```javascript
// 标题匹配
const titleMatch = note.title.includes(searchValue);

// 标签匹配
const tagMatch = note.tags?.some(tag => tag.includes(searchValue));

// 任一匹配即保留
return titleMatch || tagMatch;
```

---

### 性能优化

1. **useMemo 缓存**（TagFilter）
   ```javascript
   const tagCounts = useMemo(() => {
     // 计算逻辑
   }, [userNotes, userTrashedNotes, viewTrashedNotes]);
   ```

2. **防抖保存**（NotesContext）
   ```javascript
   const timer = setTimeout(() => {
     updateNotes();
   }, 1000);
   ```

---

## 测试检查清单

- [ ] 添加标签（回车、逗号）
- [ ] 删除标签（X 按钮、Backspace）
- [ ] 标签去重（大小写不敏感）
- [ ] 标签筛选（选中、取消）
- [ ] 搜索匹配（标题、标签）
- [ ] 自动保存（1秒延迟）
- [ ] 回收站只读模式
- [ ] 明暗主题适配
- [ ] 移动端响应式
- [ ] 笔记列表标签显示（最多3个）

---

## 常见问题

### Q: 标签不保存？
**A:** 检查：
1. 网络请求是否成功（Network 面板）
2. 是否等待1秒自动保存延迟
3. JWT token 是否有效

---

### Q: 搜索不匹配标签？
**A:** 检查：
1. 笔记是否有 tags 字段
2. tags 是否为数组类型
3. 搜索关键词是否包含在标签中

---

### Q: 旧笔记兼容性？
**A:**
- 自动兼容，使用 `|| []` 默认值
- 无需手动迁移数据
- 可随时为旧笔记添加标签

---

## 扩展建议

### 短期优化
- [ ] 添加标签颜色自定义
- [ ] 实现标签自动补全
- [ ] 限制标签长度（如20字符）

### 中期优化
- [ ] 批量标签操作
- [ ] 标签管理页面
- [ ] 多标签组合筛选（AND/OR）

### 长期优化
- [ ] 服务端搜索
- [ ] 数据库索引优化
- [ ] 标签使用统计和推荐

---

## API 说明

### 现有接口无需修改

**GET `/api/account/notes`**
- 返回包含 tags 字段的完整笔记数组
- 向后兼容（旧笔记无 tags 字段）

**PUT `/api/account/notes`**
- 接受包含 tags 字段的笔记数组
- 完整更新 notes 和 trashedNotes

**请求体示例：**
```json
{
  "notes": [
    {
      "id": "abc",
      "title": "标题",
      "body": "内容",
      "tags": ["工作", "重要"]
    }
  ],
  "trashedNotes": []
}
```

---

## 代码示例

### 使用 TagInput 组件

```jsx
import TagInput from './TagInput';

<TagInput
  tags={currentNote.tags || []}
  onChange={(newTags) => handleUpdateTags(newTags)}
  disabled={isReadOnly}
/>
```

---

### 使用 TagFilter 组件

```jsx
import TagFilter from './TagFilter';

<TagFilter />
// 自动从 NotesContext 获取数据，无需 props
```

---

### 在 NotesContext 中使用

```jsx
const {
  selectedTag,       // 当前选中的标签
  setSelectedTag,    // 设置选中标签
  handleTagsChange,  // 更新笔记标签
  filteredNotes      // 过滤后的笔记（包含标签筛选）
} = useContext(NotesContext);
```

---

## 文件大小统计

- TagInput.js: ~100 行
- TagFilter.js: ~80 行
- NotesContext.js: +60 行（修改）
- NotePreviewer.js: +30 行（修改）
- Recipient.js: +5 行（修改）
- NoteInRecipient.js: +25 行（修改）
- initialAccountNote.js: +5 行（修改）

**总计：** ~305 行新增/修改代码

---

## 版本信息

- **功能版本：** 1.0
- **开发时间：** 2025年
- **兼容性：** Next.js 12+, React 18+
- **浏览器支持：** 现代浏览器（Chrome 90+, Firefox 88+, Safari 14+）

---

## 下一步行动

1. **部署前测试：**
   - 运行 `npm run dev`
   - 完成测试检查清单
   - 验证明暗主题
   - 测试移动端响应式

2. **生产部署：**
   - 运行 `npm run build`
   - 检查构建错误
   - 部署到 Vercel 或其他平台

3. **监控：**
   - 观察用户反馈
   - 监控性能指标
   - 收集改进建议

---

**文档创建：** 2025年
**最后更新：** 2025年

如有问题，请参考完整的《标签系统开发文档.md》
