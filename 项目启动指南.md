# Notella 项目启动指南

## 🚀 快速启动（5分钟）

### 前置要求

确保你的电脑已安装：
- **Node.js** 14.0 或更高版本
- **npm** 或 **yarn** 包管理器
- **MongoDB** 数据库（本地或云端）

### 检查 Node.js 版本
```bash
node --version  # 应该显示 v14.x.x 或更高
npm --version   # 应该显示 6.x.x 或更高
```

---

## 📋 详细步骤

### 步骤 1：安装依赖

在项目根目录（notella 文件夹）下运行：

```bash
npm install
```

**或者使用 yarn：**
```bash
yarn install
```

**预期结果：**
- 安装过程需要 1-3 分钟
- 会创建 `node_modules` 文件夹
- 控制台显示安装成功信息

---

### 步骤 2：配置 MongoDB 数据库

#### 选项 A：使用 MongoDB Atlas（云端，推荐）

1. **注册账户**
   - 访问 https://www.mongodb.com/cloud/atlas
   - 创建免费账户

2. **创建集群**
   - 选择 "Shared Cluster"（免费）
   - 选择离你最近的区域
   - 等待集群创建（约 3-5 分钟）

3. **获取连接字符串**
   - 点击 "Connect" → "Connect your application"
   - 复制连接字符串，格式如：
     ```
     mongodb+srv://username:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
     ```
   - 将 `<password>` 替换为你的实际密码

4. **配置网络访问**
   - 在 "Network Access" 中添加 IP 地址
   - 可以选择 "Allow Access from Anywhere"（0.0.0.0/0）用于开发

#### 选项 B：使用本地 MongoDB

1. **安装 MongoDB**
   - macOS: `brew install mongodb-community`
   - Windows: 下载安装包 https://www.mongodb.com/try/download/community
   - Linux: 参考官方文档

2. **启动 MongoDB**
   ```bash
   # macOS/Linux
   mongod --dbpath ~/data/db

   # 或者作为服务启动（macOS）
   brew services start mongodb-community
   ```

3. **连接字符串**
   ```
   mongodb://localhost:27017/notella
   ```

---

### 步骤 3：创建环境变量文件

在项目根目录创建 `.env.local` 文件：

```bash
touch .env.local
```

**编辑 `.env.local` 文件，添加以下内容：**

```env
# MongoDB 连接字符串
MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/notella?retryWrites=true&w=majority

# JWT 密钥（用于加密用户 token）
ACCESS_TOKEN_SECRET=your-super-secret-key-here-change-this-in-production
```

**重要提示：**
1. **MONGODB_URI：** 替换为你实际的 MongoDB 连接字符串
2. **ACCESS_TOKEN_SECRET：** 生成一个随机字符串，建议至少 32 位

**生成安全的密钥：**
```bash
# 在终端运行（生成 32 字节随机字符串）
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**示例配置：**
```env
MONGODB_URI=mongodb+srv://myuser:MyPassword123@cluster0.abcde.mongodb.net/notella?retryWrites=true&w=majority
ACCESS_TOKEN_SECRET=8f3d9c2a1b7e6f5d4c3b2a1098765432fedcba0987654321abcdef1234567890
```

---

### 步骤 4：启动开发服务器

```bash
npm run dev
```

**或者使用 yarn：**
```bash
yarn dev
```

**预期输出：**
```
ready - started server on 0.0.0.0:3000, url: http://localhost:3000
event - compiled client and server successfully in 2.5s (165 modules)
wait  - compiling...
event - compiled successfully in 250 ms (152 modules)
```

---

### 步骤 5：访问应用

打开浏览器，访问：
```
http://localhost:3000
```

**你应该看到：**
- Notella 登录页面
- 可以点击 "Register" 注册新账户

---

## ✅ 验证安装

### 测试流程

1. **注册账户**
   - 输入邮箱（如 test@example.com）
   - 输入密码（至少 8 位）
   - 点击 "Register"

2. **登录**
   - 使用刚才注册的邮箱和密码
   - 点击 "Login"

3. **查看欢迎笔记**
   - 应该看到左侧有一个 "Readme" 笔记
   - 笔记带有标签：guide, documentation, welcome

4. **测试标签功能**
   - 点击 "Readme" 笔记
   - 滚动到底部查看标签输入区域
   - 尝试添加新标签

---

## 🐛 常见问题排查

### 问题 1：`npm install` 失败

**错误信息：**
```
npm ERR! code EACCES
```

**解决方案：**
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

---

### 问题 2：无法连接 MongoDB

**错误信息：**
```
Error while attempting to connect to database.
```

**检查清单：**
1. ✓ `.env.local` 文件是否存在
2. ✓ `MONGODB_URI` 是否正确
3. ✓ 密码中的特殊字符是否需要 URL 编码
4. ✓ MongoDB Atlas 的网络访问是否配置正确
5. ✓ 本地 MongoDB 是否正在运行

**特殊字符编码：**
如果密码包含特殊字符，需要 URL 编码：
- `@` → `%40`
- `:` → `%3A`
- `/` → `%2F`
- 示例：`Pass@123` → `Pass%40123`

---

### 问题 3：端口 3000 已被占用

**错误信息：**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**解决方案 A：** 停止占用端口的进程
```bash
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

**解决方案 B：** 使用其他端口
```bash
PORT=3001 npm run dev
```

---

### 问题 4：页面无法加载

**检查清单：**
1. 确认开发服务器正在运行
2. 检查控制台是否有错误
3. 尝试清除浏览器缓存
4. 尝试使用隐私模式/无痕模式

---

### 问题 5：样式显示异常

**可能原因：** TailwindCSS 未正确编译

**解决方案：**
```bash
# 停止开发服务器（Ctrl+C）
# 删除 .next 文件夹
rm -rf .next

# 重新启动
npm run dev
```

---

## 🔧 开发工具推荐

### VS Code 插件
- **ES7+ React/Redux/React-Native snippets** - React 代码片段
- **Tailwind CSS IntelliSense** - TailwindCSS 自动补全
- **ESLint** - 代码检查
- **Prettier** - 代码格式化

### 浏览器插件
- **React Developer Tools** - React 组件调试
- **Redux DevTools** - 状态管理调试（虽然本项目使用 Context）

---

## 📁 项目结构说明

```
notella/
├── components/          # React 组件
│   ├── TagInput.js     # 标签输入组件（新增）
│   ├── TagFilter.js    # 标签筛选组件（新增）
│   ├── NotePreviewer.js
│   ├── Recipient.js
│   └── ...
├── context/            # React Context 状态管理
│   ├── NotesContext.js # 笔记状态管理（已修改）
│   └── LayoutContext.js
├── database/           # 数据库相关
│   ├── models/         # Mongoose 模型
│   ├── middleware/     # API 中间件
│   └── connection.js   # 数据库连接
├── pages/              # Next.js 页面路由
│   ├── api/           # API 路由
│   ├── index.js       # 主页
│   ├── login.js       # 登录页
│   └── register.js    # 注册页
├── public/            # 静态资源
├── styles/            # CSS 样式
├── utils/             # 工具函数
│   └── initialAccountNote.js  # 初始笔记（已修改）
├── .env.local         # 环境变量（需要创建）
├── package.json       # 依赖配置
└── next.config.js     # Next.js 配置
```

---

## 🎯 下一步行动

启动成功后，你可以：

1. **熟悉应用功能**
   - 创建、编辑、删除笔记
   - 添加标签
   - 尝试标签筛选和搜索

2. **查看代码**
   - 阅读《标签系统开发文档.md》了解实现细节
   - 查看《标签系统-快速参考.md》了解快速参考

3. **自定义开发**
   - 根据需求修改样式
   - 添加新功能
   - 优化性能

4. **部署上线**
   - 参考下面的生产部署指南

---

## 🚀 生产部署（可选）

### 使用 Vercel（推荐）

1. **安装 Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **登录 Vercel**
   ```bash
   vercel login
   ```

3. **部署项目**
   ```bash
   vercel
   ```

4. **配置环境变量**
   - 在 Vercel 控制台中添加环境变量
   - `MONGODB_URI`
   - `ACCESS_TOKEN_SECRET`

5. **访问部署的应用**
   - Vercel 会提供一个 URL
   - 例如：https://notella-xxx.vercel.app

---

## 📝 环境变量完整说明

| 变量名 | 必需 | 说明 | 示例 |
|--------|------|------|------|
| `MONGODB_URI` | ✅ | MongoDB 连接字符串 | `mongodb+srv://user:pass@cluster.mongodb.net/notella` |
| `ACCESS_TOKEN_SECRET` | ✅ | JWT 签名密钥（至少32位） | `8f3d9c2a1b7e6f5d4c3b2a1098765432...` |

---

## 🔒 安全建议

### 开发环境
- ✅ 使用 `.env.local`（已被 .gitignore 忽略）
- ✅ 不要将 `.env.local` 提交到 Git
- ✅ 使用复杂的 `ACCESS_TOKEN_SECRET`

### 生产环境
- ✅ 使用强密码
- ✅ 定期更换密钥
- ✅ 限制 MongoDB 网络访问
- ✅ 启用 HTTPS

---

## 📞 获取帮助

如果遇到问题：

1. **检查控制台错误**
   - 浏览器开发者工具（F12）
   - 终端输出

2. **查看文档**
   - 《标签系统开发文档.md》
   - 《标签系统-快速参考.md》

3. **检查 GitHub Issues**
   - https://github.com/jacksonpf1/notella/issues

---

## ✨ 快速命令参考

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 运行生产服务器
npm start

# 代码检查
npm run lint

# 清理缓存
rm -rf .next node_modules
npm install
```

---

**祝你使用愉快！** 🎉

如有问题，请随时查阅本指南或相关文档。
